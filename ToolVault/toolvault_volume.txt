=== START TOOL 001 — web_search ===
NAME: web_search
DESCRIPTION: Search the web using Perplexity Sonar AI search. Returns a synthesized answer with source citations.
CATEGORY: web
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  query (string, required): The search query to look up on the web
  search_recency_filter (string, optional, default="month", enum=[hour,day,week,month,year]): Filter results by recency: 'day', 'week', 'month', 'year' (default: 'month')
RETURNS: Returns a synthesized answer with source citations
EXAMPLE: web_search(query="...")
JSON_SCHEMA:
{"type":"object","properties":{"query":{"type":"string","description":"The search query to look up on the web"},"search_recency_filter":{"type":"string","description":"Filter results by recency: 'day', 'week', 'month', 'year' (default: 'month')","enum":["hour","day","week","month","year"],"default":"month"}},"required":["query"]}
=== END TOOL 001 — web_search ===
=== START TOOL 002 — web_fetch ===
NAME: web_fetch
DESCRIPTION: Fetch and extract clean content from a specific URL. Use this to read articles, documentation, blog posts, or web pages when you have the URL.
CATEGORY: web
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  url (string, required): The complete URL to fetch (must start with http:// or https://)
  max_chars (integer, optional, default="80000"): Maximum characters to return (default 80000, optional)
EXAMPLE: web_fetch(url="...")
JSON_SCHEMA:
{"type":"object","properties":{"url":{"type":"string","description":"The complete URL to fetch (must start with http:// or https://)"},"max_chars":{"type":"integer","description":"Maximum characters to return (default 80000, optional)","default":80000}},"required":["url"]}
=== END TOOL 002 — web_fetch ===
=== START TOOL 003 — generate_image ===
NAME: generate_image
DESCRIPTION: Generate images using Gemini's Nano Banana Pro (gemini-3-pro-image-preview). Supports text-to-image AND image-to-image. For image-to-image, pass reference_images URLs from previous generations to guide style/content. Returns a task_id that completes asynchronously.
CATEGORY: media_generation
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  prompt (string, required): Detailed description of the image to generate. Be specific about subjects, style, composition, lighting, colors, and mood.
  reference_images (array, optional): URLs of reference images for style/content guidance (e.g., ['/ui/uploads/sunset_abc123.png']). Use for image-to-image generation. Max 10 images recommended.
  aspectRatio (string, optional, default="16:9", enum=[1:1,16:9,9:16,4:3,3:4]): Aspect ratio: '1:1' (square), '16:9' (landscape), '9:16' (portrait), '4:3', '3:4'. Default: '16:9'
  resolution (string, optional, default="1K", enum=[1K,2K,4K]): Image resolution: '1K' (1024px), '2K' (2048px), '4K' (4096px). Higher = more detail but slower. Default: '1K'
  numberOfImages (integer, optional, default="1"): Number of images to generate (1-4). Default: 1
RETURNS: task_id (string) for async status tracking via get_task_status
EXAMPLE: generate_image(prompt="...")
NOTES: Async operation - use get_task_status to check progress.
JSON_SCHEMA:
{"type":"object","properties":{"prompt":{"type":"string","description":"Detailed description of the image to generate. Be specific about subjects, style, composition, lighting, colors, and mood."},"reference_images":{"type":"array","items":{"type":"string"},"description":"URLs of reference images for style/content guidance (e.g., ['/ui/uploads/sunset_abc123.png']). Use for image-to-image generation. Max 10 images recommended.","maxItems":14},"aspectRatio":{"type":"string","description":"Aspect ratio: '1:1' (square), '16:9' (landscape), '9:16' (portrait), '4:3', '3:4'. Default: '16:9'","enum":["1:1","16:9","9:16","4:3","3:4"],"default":"16:9"},"resolution":{"type":"string","description":"Image resolution: '1K' (1024px), '2K' (2048px), '4K' (4096px). Higher = more detail but slower. Default: '1K'","enum":["1K","2K","4K"],"default":"1K"},"numberOfImages":{"type":"integer","description":"Number of images to generate (1-4). Default: 1","minimum":1,"maximum":4,"default":1}},"required":["prompt"]}
=== END TOOL 003 — generate_image ===
=== START TOOL 004 — generate_video ===
NAME: generate_video
DESCRIPTION: Generate videos using Google's Veo 3.1. Supports THREE modes: (1) text-to-video (just prompt), (2) image-to-video (pass image_url to animate an image), (3) video-extension (pass video_url to extend/continue an existing video). Returns a task_id (takes 5-20 minutes).
CATEGORY: media_generation
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  prompt (string, required): For text-to-video: full video description. For image-to-video: describe how the image should animate. For video-extension: describe what happens NEXT in the video.
  image_url (string, optional): URL of an image to animate (e.g., /ui/uploads/sunset_abc123.png). If provided, creates image-to-video instead of text-to-video.
  video_url (string, optional): URL of a video to EXTEND. If provided, creates a continuation of that video guided by the prompt. Takes priority over image_url.
  aspectRatio (string, optional, default="16:9", enum=[16:9,9:16]): Aspect ratio: '16:9' (landscape) or '9:16' (portrait). Default: '16:9'
  duration (integer, optional, default="8", enum=[4,6,8]): Video duration in seconds: 4, 6, or 8. Default: 8
  resolution (string, optional, default="720p", enum=[720p,1080p]): Video resolution: '720p' or '1080p'. IMPORTANT: Video extension (video_url) REQUIRES '720p'. Default: '720p'
  negativePrompt (string, optional): Optional. Things to exclude from the video
RETURNS: task_id (string) for async status tracking via get_task_status
EXAMPLE: generate_video(prompt="...")
NOTES: Async operation - use get_task_status to check progress.
JSON_SCHEMA:
{"type":"object","properties":{"prompt":{"type":"string","description":"For text-to-video: full video description. For image-to-video: describe how the image should animate. For video-extension: describe what happens NEXT in the video."},"image_url":{"type":"string","description":"URL of an image to animate (e.g., /ui/uploads/sunset_abc123.png). If provided, creates image-to-video instead of text-to-video."},"video_url":{"type":"string","description":"URL of a video to EXTEND. If provided, creates a continuation of that video guided by the prompt. Takes priority over image_url."},"aspectRatio":{"type":"string","description":"Aspect ratio: '16:9' (landscape) or '9:16' (portrait). Default: '16:9'","enum":["16:9","9:16"],"default":"16:9"},"duration":{"type":"integer","description":"Video duration in seconds: 4, 6, or 8. Default: 8","enum":[4,6,8],"default":8},"resolution":{"type":"string","description":"Video resolution: '720p' or '1080p'. IMPORTANT: Video extension (video_url) REQUIRES '720p'. Default: '720p'","enum":["720p","1080p"],"default":"720p"},"negativePrompt":{"type":"string","description":"Optional. Things to exclude from the video"}},"required":["prompt"]}
=== END TOOL 004 — generate_video ===
=== START TOOL 005 — generate_music ===
NAME: generate_music
DESCRIPTION: Generate 30-second instrumental music using Lyria-002. CRITICAL: Translate user requests into allowed vocabulary. The API REJECTS genre names, style words, and artist references. ONLY USE: instrument names + tempo + texture. Example: 'EDM' -> 'Synthesizer arpeggios with pulsing bass, fast driving tempo'.
CATEGORY: media_generation
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  prompt (string, required): TRANSLATED description using ONLY: Instruments (piano, guitar, drums, bass, synth, strings, brass, etc.) + Tempo (fast, slow, moderate, driving, laid-back) + Texture (warm, bright, dark, smooth, aggressive, dense, sparse). FORBIDDEN: genre names, style words, artist names.
  negativePrompt (string, optional): Things to exclude (e.g., 'vocals, singing')
  sampleCount (integer, optional): Variations to generate (1-4, default 1)
EXAMPLE: generate_music(prompt="...")
JSON_SCHEMA:
{"type":"object","properties":{"prompt":{"type":"string","description":"TRANSLATED description using ONLY: Instruments (piano, guitar, drums, bass, synth, strings, brass, etc.) + Tempo (fast, slow, moderate, driving, laid-back) + Texture (warm, bright, dark, smooth, aggressive, dense, sparse). FORBIDDEN: genre names, style words, artist names."},"negativePrompt":{"type":"string","description":"Things to exclude (e.g., 'vocals, singing')"},"sampleCount":{"type":"integer","description":"Variations to generate (1-4, default 1)"}},"required":["prompt"]}
=== END TOOL 005 — generate_music ===
=== START TOOL 006 — extend_video ===
NAME: extend_video
DESCRIPTION: Extend an existing video using Veo 3.1. Takes 5-20 minutes. Returns task_id (async). The new clip continues from where the original ended.
CATEGORY: media_generation
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  video_url (string, required): URL of the video to extend (e.g. /ui/uploads/video.mp4)
  prompt (string, optional): Optional prompt for how to extend the video
RETURNS: task_id (string) for async status tracking via get_task_status
EXAMPLE: extend_video(video_url="...")
NOTES: Async operation - use get_task_status to check progress.
JSON_SCHEMA:
{"type":"object","properties":{"video_url":{"type":"string","description":"URL of the video to extend (e.g. /ui/uploads/video.mp4)"},"prompt":{"type":"string","description":"Optional prompt for how to extend the video"}},"required":["video_url"]}
=== END TOOL 006 — extend_video ===
=== START TOOL 007 — get_media ===
NAME: get_media
DESCRIPTION: Retrieve a previously generated media file (image, video, music) by URL or task_id. Use to verify media exists before using it, or to get metadata about past generations.
CATEGORY: media_management
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  url (string, optional): Media URL (e.g., /ui/uploads/sunset_abc123.png from a previous generation)
  task_id (string, optional): Alternative: Task ID from a previous generation (will look up the URL)
RETURNS: task_id (string) for async status tracking via get_task_status
EXAMPLE: get_media()
NOTES: Async operation - use get_task_status to check progress.
JSON_SCHEMA:
{"type":"object","properties":{"url":{"type":"string","description":"Media URL (e.g., /ui/uploads/sunset_abc123.png from a previous generation)"},"task_id":{"type":"string","description":"Alternative: Task ID from a previous generation (will look up the URL)"}},"required":[]}
=== END TOOL 007 — get_media ===
=== START TOOL 008 — list_media ===
NAME: list_media
DESCRIPTION: List media files (images, videos, audio) in the uploads folder. Use to see what media is available for image-to-video, video extension, or image-to-image.
CATEGORY: media_management
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone
PARAMETERS:
  media_type (string, optional, enum=[image,video,audio]): Filter by type: 'image', 'video', 'audio', or omit for all types
  limit (integer, optional, default="20"): Maximum number of results (default 20, max 50)
RETURNS: List of matching results
EXAMPLE: list_media()
JSON_SCHEMA:
{"type":"object","properties":{"media_type":{"type":"string","description":"Filter by type: 'image', 'video', 'audio', or omit for all types","enum":["image","video","audio"]},"limit":{"type":"integer","description":"Maximum number of results (default 20, max 50)","default":20}},"required":[]}
=== END TOOL 008 — list_media ===
=== START TOOL 009 — search_media ===
NAME: search_media
DESCRIPTION: Search for media files by description, prompt, or filename. Use when you need to find a specific image/video/audio that was previously generated or uploaded.
CATEGORY: media_management
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone
PARAMETERS:
  query (string, required): Search query - matches against prompts, descriptions, and filenames
  media_type (string, optional, enum=[image,video,audio]): Filter by type: 'image', 'video', 'audio', or omit for all types
  limit (integer, optional, default="10"): Maximum number of results (default 10)
RETURNS: List of matching results
EXAMPLE: search_media(query="...")
JSON_SCHEMA:
{"type":"object","properties":{"query":{"type":"string","description":"Search query - matches against prompts, descriptions, and filenames"},"media_type":{"type":"string","description":"Filter by type: 'image', 'video', 'audio', or omit for all types","enum":["image","video","audio"]},"limit":{"type":"integer","description":"Maximum number of results (default 10)","default":10}},"required":["query"]}
=== END TOOL 009 — search_media ===
=== START TOOL 010 — search_snapshots ===
NAME: search_snapshots
DESCRIPTION: Your primary memory tool. The BlackBox contains 1,600+ snapshots — an unlimited bucket of memory holding every past conversation, development session, bug fix, decision, user preference, and interaction. Search this FIRST and OFTEN. Everything about the user, their projects, past work, preferences, and recent activity lives here. When in doubt, search.
CATEGORY: memory
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  query (string, required): Natural language search query. Be descriptive — 'CSS design token migration' finds more than 'CSS'. Search multiple times with different queries if needed.
  operator (string, optional): Operator/user name to search within (default: all operators)
  limit (integer, optional, default="10"): Maximum number of results (default: 10)
RETURNS: List of matching results
EXAMPLE: search_snapshots(query="...")
JSON_SCHEMA:
{"type":"object","properties":{"query":{"type":"string","description":"Natural language search query. Be descriptive \u2014 'CSS design token migration' finds more than 'CSS'. Search multiple times with different queries if needed."},"operator":{"type":"string","description":"Operator/user name to search within (default: all operators)"},"limit":{"type":"integer","description":"Maximum number of results (default: 10)","default":10}},"required":["query"]}
=== END TOOL 010 — search_snapshots ===
=== START TOOL 011 — get_snapshot ===
NAME: get_snapshot
DESCRIPTION: Retrieve a specific snapshot by ID from the BlackBox memory. Use when you have a snapshot ID from search results and need the full content.
CATEGORY: memory
GROUPS: chat, chat_cu, phone, mcp
PARAMETERS:
  snap_id (string, required): Snapshot ID (e.g., SNAP-20260208-1234)
  include_content (boolean, optional, default="True"): Include full content (default: true)
RETURNS: Requested data object
EXAMPLE: get_snapshot(snap_id="...")
JSON_SCHEMA:
{"type":"object","properties":{"snap_id":{"type":"string","description":"Snapshot ID (e.g., SNAP-20260208-1234)"},"include_content":{"type":"boolean","description":"Include full content (default: true)","default":true}},"required":["snap_id"]}
=== END TOOL 011 — get_snapshot ===
=== START TOOL 012 — list_recent_snapshots ===
NAME: list_recent_snapshots
DESCRIPTION: Get the most recent snapshots. IMPORTANT: Use this at the START of outbound calls to check for context from other models - there may be task details, order information, or instructions provided before this call was initiated.
CATEGORY: memory
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  operator (string, optional): Operator/user name to filter snapshots for
  count (integer, optional, default="3"): Number of recent snapshots to retrieve (default: 3, max: 5)
RETURNS: List of matching results
EXAMPLE: list_recent_snapshots()
JSON_SCHEMA:
{"type":"object","properties":{"operator":{"type":"string","description":"Operator/user name to filter snapshots for"},"count":{"type":"integer","description":"Number of recent snapshots to retrieve (default: 3, max: 5)","default":3}},"required":[]}
=== END TOOL 012 — list_recent_snapshots ===
=== START TOOL 013 — get_current_time ===
NAME: get_current_time
DESCRIPTION: Get the current date and time with timezone information.
CATEGORY: memory
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
RETURNS: Requested data object
EXAMPLE: get_current_time()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 013 — get_current_time ===
=== START TOOL 014 — send_sms ===
NAME: send_sms
DESCRIPTION: Send an SMS text message via the TG200 cellular gateway. The message is delivered as a real cellular SMS. For long messages, the system automatically splits into multiple SMS segments (160 chars each, up to 10 segments). Use search_contacts first to find the recipient's number.
CATEGORY: communication
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone
PARAMETERS:
  phone_number (string, required): Phone number in E.164 format (e.g., +15551234567) or 10-digit US format
  message (string, required): The text message to send. Plain text only, no markdown. Max ~1500 characters (auto-split into 160-char segments).
EXAMPLE: send_sms(phone_number="...", message="...")
NOTES: Look up contact number with search_contacts first.
JSON_SCHEMA:
{"type":"object","properties":{"phone_number":{"type":"string","description":"Phone number in E.164 format (e.g., +15551234567) or 10-digit US format"},"message":{"type":"string","description":"The text message to send. Plain text only, no markdown. Max ~1500 characters (auto-split into 160-char segments)."}},"required":["phone_number","message"]}
=== END TOOL 014 — send_sms ===
=== START TOOL 015 — make_phone_call ===
NAME: make_phone_call
DESCRIPTION: DELEGATE a real phone call to a separate AI voice agent via the TG200 cellular gateway. You are NOT making the call yourself — you are BRIEFING another AI agent who will have a real-time voice conversation on your behalf. Use 'role' to define WHO the agent IS (their persona/character), and 'greeting' for WHAT task to accomplish on this call. Use search_contacts first to find the number.
CATEGORY: communication
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone
PARAMETERS:
  phone_number (string, required): Phone number in E.164 format (e.g., +15551234567) or 10-digit US format
  role (string, optional): The PERSONA/CHARACTER for the AI voice agent — define WHO they are. This becomes the agent's system prompt before the call starts.
  greeting (string, optional): The TASK INSTRUCTIONS — WHAT to do on this specific call. The voice agent speaks this greeting first, then has a free-form conversation.
  backend (string, optional, enum=[openai_realtime,gemini_live,grok_live]): AI voice backend for the real-time conversation
EXAMPLE: make_phone_call(phone_number="...")
NOTES: Look up contact number with search_contacts first.
JSON_SCHEMA:
{"type":"object","properties":{"phone_number":{"type":"string","description":"Phone number in E.164 format (e.g., +15551234567) or 10-digit US format"},"role":{"type":"string","description":"The PERSONA/CHARACTER for the AI voice agent \u2014 define WHO they are. This becomes the agent's system prompt before the call starts."},"greeting":{"type":"string","description":"The TASK INSTRUCTIONS \u2014 WHAT to do on this specific call. The voice agent speaks this greeting first, then has a free-form conversation."},"backend":{"type":"string","description":"AI voice backend for the real-time conversation","enum":["openai_realtime","gemini_live","grok_live"]}},"required":["phone_number"]}
=== END TOOL 015 — make_phone_call ===
=== START TOOL 016 — make_voice_call ===
NAME: make_voice_call
DESCRIPTION: Call a phone number and deliver a pre-recorded voice message using TTS. The system generates the speech audio first (via OpenAI TTS), then calls the number and plays it. For interactive two-way calls, use make_phone_call instead.
CATEGORY: communication
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone
PARAMETERS:
  phone_number (string, required): Phone number in E.164 format (e.g., +15551234567) or 10-digit US format
  message (string, required): The message to speak. Will be converted to speech via TTS before calling.
  voice (string, optional, enum=[alloy,echo,fable,onyx,nova,shimmer]): TTS voice to use for the message
EXAMPLE: make_voice_call(phone_number="...", message="...")
JSON_SCHEMA:
{"type":"object","properties":{"phone_number":{"type":"string","description":"Phone number in E.164 format (e.g., +15551234567) or 10-digit US format"},"message":{"type":"string","description":"The message to speak. Will be converted to speech via TTS before calling."},"voice":{"type":"string","description":"TTS voice to use for the message","enum":["alloy","echo","fable","onyx","nova","shimmer"]}},"required":["phone_number","message"]}
=== END TOOL 016 — make_voice_call ===
=== START TOOL 017 — search_contacts ===
NAME: search_contacts
DESCRIPTION: Search the contact book for people by name, phone number, tag, or keyword. Use this before making calls or sending texts to find the person's number.
CATEGORY: contacts
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone
PARAMETERS:
  query (string, required): Name, phone number, tag, or any search term to find contacts
RETURNS: List of matching results
EXAMPLE: search_contacts(query="...")
JSON_SCHEMA:
{"type":"object","properties":{"query":{"type":"string","description":"Name, phone number, tag, or any search term to find contacts"}},"required":["query"]}
=== END TOOL 017 — search_contacts ===
=== START TOOL 018 — save_contact ===
NAME: save_contact
DESCRIPTION: Save a new contact or update an existing one in the contact book. Use when a user mentions someone new with contact info.
CATEGORY: contacts
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone
PARAMETERS:
  name (string, required): Full name of the contact
  notes (string, required): Context about who this person is and any relevant details
  tags (array, required): Categorization tags (e.g., family, work, doctor, vip)
  phone (string, optional): Phone number in E.164 format (e.g., +15551234567)
  email (string, optional): Email address
  relationship (string, optional): Relationship to the user (e.g., friend, coworker, doctor)
EXAMPLE: save_contact(name="...", notes="...", tags="...")
JSON_SCHEMA:
{"type":"object","properties":{"name":{"type":"string","description":"Full name of the contact"},"notes":{"type":"string","description":"Context about who this person is and any relevant details"},"tags":{"type":"array","items":{"type":"string"},"description":"Categorization tags (e.g., family, work, doctor, vip)"},"phone":{"type":"string","description":"Phone number in E.164 format (e.g., +15551234567)"},"email":{"type":"string","description":"Email address"},"relationship":{"type":"string","description":"Relationship to the user (e.g., friend, coworker, doctor)"}},"required":["name","notes","tags"]}
=== END TOOL 018 — save_contact ===
=== START TOOL 019 — create_cron_job ===
NAME: create_cron_job
DESCRIPTION: Create a scheduled task (cron job) that runs a prompt on a schedule. Use when the user wants recurring reminders, checks, or automated tasks.
CATEGORY: scheduling
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone
PARAMETERS:
  name (string, required): A short, descriptive name for this task (e.g., 'Morning Server Check')
  prompt (string, required): The prompt/instruction that will be sent to the AI when the job fires
  schedule (string, required): Cron expression (e.g., '0 7 * * *' for daily at 7 AM, '*/30 * * * *' for every 30 minutes)
  frequency_hint (string, optional): Human-readable description of the schedule (e.g., 'Every day at 7 AM')
  model (string, optional, enum=[gemini,openai,claude,grok,computer-use]): Which AI model should execute this job. Use 'computer-use' for tasks needing desktop/browser/GUI interaction (default: gemini)
  delivery (string, optional, enum=[snapshot,sms,voice_call,notification]): How to deliver the result (default: snapshot)
  delivery_target (string, optional): Phone number for SMS/voice delivery (E.164 format), not needed for snapshot/notification
  one_shot (boolean, optional): If true, run once and auto-delete (for one-time reminders). Default: false
EXAMPLE: create_cron_job(name="...", prompt="...", schedule="...")
JSON_SCHEMA:
{"type":"object","properties":{"name":{"type":"string","description":"A short, descriptive name for this task (e.g., 'Morning Server Check')"},"prompt":{"type":"string","description":"The prompt/instruction that will be sent to the AI when the job fires"},"schedule":{"type":"string","description":"Cron expression (e.g., '0 7 * * *' for daily at 7 AM, '*/30 * * * *' for every 30 minutes)"},"frequency_hint":{"type":"string","description":"Human-readable description of the schedule (e.g., 'Every day at 7 AM')"},"model":{"type":"string","description":"Which AI model should execute this job. Use 'computer-use' for tasks needing desktop/browser/GUI interaction (default: gemini)","enum":["gemini","openai","claude","grok","computer-use"]},"delivery":{"type":"string","description":"How to deliver the result (default: snapshot)","enum":["snapshot","sms","voice_call","notification"]},"delivery_target":{"type":"string","description":"Phone number for SMS/voice delivery (E.164 format), not needed for snapshot/notification"},"one_shot":{"type":"boolean","description":"If true, run once and auto-delete (for one-time reminders). Default: false"}},"required":["name","prompt","schedule"]}
=== END TOOL 019 — create_cron_job ===
=== START TOOL 020 — edit_cron_job ===
NAME: edit_cron_job
DESCRIPTION: Edit an existing scheduled task (cron job). Change the prompt, schedule, delivery method, or other settings.
CATEGORY: scheduling
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone
PARAMETERS:
  job_id (string, required): The ID of the cron job to edit
  name (string, optional): New name for the job
  prompt (string, optional): New prompt/instruction for the job
  schedule (string, optional): New cron expression
  frequency_hint (string, optional): New human-readable schedule description
  model (string, optional, enum=[gemini,openai,claude,grok,computer-use]): Change which AI model executes this job
  delivery (string, optional, enum=[snapshot,sms,voice_call,notification]): Change how results are delivered
  delivery_target (string, optional): New phone number for SMS/voice delivery
  pause (boolean, optional): Set to true to pause the job, false to resume it
EXAMPLE: edit_cron_job(job_id="...")
JSON_SCHEMA:
{"type":"object","properties":{"job_id":{"type":"string","description":"The ID of the cron job to edit"},"name":{"type":"string","description":"New name for the job"},"prompt":{"type":"string","description":"New prompt/instruction for the job"},"schedule":{"type":"string","description":"New cron expression"},"frequency_hint":{"type":"string","description":"New human-readable schedule description"},"model":{"type":"string","description":"Change which AI model executes this job","enum":["gemini","openai","claude","grok","computer-use"]},"delivery":{"type":"string","description":"Change how results are delivered","enum":["snapshot","sms","voice_call","notification"]},"delivery_target":{"type":"string","description":"New phone number for SMS/voice delivery"},"pause":{"type":"boolean","description":"Set to true to pause the job, false to resume it"}},"required":["job_id"]}
=== END TOOL 020 — edit_cron_job ===
=== START TOOL 021 — search_cron_jobs ===
NAME: search_cron_jobs
DESCRIPTION: Search and list scheduled tasks (cron jobs). Use when the user asks about their scheduled tasks, reminders, or cron jobs.
CATEGORY: scheduling
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone
PARAMETERS:
  query (string, optional): Optional search query to filter jobs by name or prompt content
  status (string, optional, enum=[active,paused,all]): Filter by status (default: all)
RETURNS: List of matching results
EXAMPLE: search_cron_jobs()
JSON_SCHEMA:
{"type":"object","properties":{"query":{"type":"string","description":"Optional search query to filter jobs by name or prompt content"},"status":{"type":"string","description":"Filter by status (default: all)","enum":["active","paused","all"]}},"required":[]}
=== END TOOL 021 — search_cron_jobs ===
=== START TOOL 022 — use_computer ===
NAME: use_computer
DESCRIPTION: Control the computer using an AI agent powered by Claude Opus 4.6. Can browse the web, use desktop apps, run terminal commands, manage files — full Linux desktop access. Returns a task ID - use get_task_status to check progress.
CATEGORY: computer_control
GROUPS: chat, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  prompt (string, required): What to do on the computer (e.g., 'Go to news.ycombinator.com and find the top 3 stories')
  url (string, optional): Starting URL to navigate to (optional, defaults to about:blank)
  device_id (string, optional): Target device ID from the Tailscale mesh (default: 'blackbox' for local machine). Use list_devices to see available devices.
RETURNS: task_id (string) for async status tracking via get_task_status
EXAMPLE: use_computer(prompt="...")
JSON_SCHEMA:
{"type":"object","properties":{"prompt":{"type":"string","description":"What to do on the computer (e.g., 'Go to news.ycombinator.com and find the top 3 stories')"},"url":{"type":"string","description":"Starting URL to navigate to (optional, defaults to about:blank)"},"device_id":{"type":"string","description":"Target device ID from the Tailscale mesh (default: 'blackbox' for local machine). Use list_devices to see available devices."}},"required":["prompt"]}
=== END TOOL 022 — use_computer ===
=== START TOOL 023 — list_devices ===
NAME: list_devices
DESCRIPTION: List all devices on the Tailscale mesh network that can be controlled via Computer Use.
CATEGORY: computer_control
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  device_type (string, optional): Filter: 'android', 'linux', 'windows'
RETURNS: List of matching results
EXAMPLE: list_devices()
JSON_SCHEMA:
{"type":"object","properties":{"device_type":{"type":"string","description":"Filter: 'android', 'linux', 'windows'"}},"required":[]}
=== END TOOL 023 — list_devices ===
=== START TOOL 024 — control_android_device ===
NAME: control_android_device
DESCRIPTION: Control an Android device on the Tailscale mesh network using Gemini Computer Use. Can tap, type, swipe, open apps, and navigate the device UI autonomously. Returns a task ID - use get_task_status to check progress.
CATEGORY: computer_control
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  prompt (string, required): What to do on the Android device (e.g., 'Open the Play Store and check StoryBox reviews')
  device_id (string, required): Device ID from the device registry (e.g., 'brandon-phone'). Use list_devices to see available devices.
RETURNS: task_id (string) for async status tracking via get_task_status
EXAMPLE: control_android_device(prompt="...", device_id="...")
JSON_SCHEMA:
{"type":"object","properties":{"prompt":{"type":"string","description":"What to do on the Android device (e.g., 'Open the Play Store and check StoryBox reviews')"},"device_id":{"type":"string","description":"Device ID from the device registry (e.g., 'brandon-phone'). Use list_devices to see available devices."}},"required":["prompt","device_id"]}
=== END TOOL 024 — control_android_device ===
=== START TOOL 025 — get_task_status ===
NAME: get_task_status
DESCRIPTION: Check the status of an async generation task (image, video, music). Returns status, progress, and result URL when complete.
CATEGORY: task_management
GROUPS: chat, chat_cu, phone, mcp
PARAMETERS:
  task_id (string, required): The task ID returned by generate_image/generate_video/generate_music
RETURNS: Returns status, progress, and result URL when complete
EXAMPLE: get_task_status(task_id="...")
NOTES: Async operation - use get_task_status to check progress.
JSON_SCHEMA:
{"type":"object","properties":{"task_id":{"type":"string","description":"The task ID returned by generate_image/generate_video/generate_music"}},"required":["task_id"]}
=== END TOOL 025 — get_task_status ===
=== START TOOL 026 — analyze_image ===
NAME: analyze_image
DESCRIPTION: Analyze an image using multimodal AI. Can describe, extract text, identify objects, answer questions about the image.
CATEGORY: analysis
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  image_url (string, required): URL or local path to the image (e.g. /ui/uploads/sunset.png or https://example.com/photo.jpg)
  prompt (string, optional): Question or instruction about the image (default: 'Describe this image in detail')
EXAMPLE: analyze_image(image_url="...")
JSON_SCHEMA:
{"type":"object","properties":{"image_url":{"type":"string","description":"URL or local path to the image (e.g. /ui/uploads/sunset.png or https://example.com/photo.jpg)"},"prompt":{"type":"string","description":"Question or instruction about the image (default: 'Describe this image in detail')"}},"required":["image_url"]}
=== END TOOL 026 — analyze_image ===
=== START TOOL 027 — analyze_audio ===
NAME: analyze_audio
DESCRIPTION: Analyze audio content using Gemini. Can transcribe, describe sounds, identify music, etc. Returns task_id (async).
CATEGORY: analysis
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  file_path (string, required): Path to the audio file to analyze
  prompt (string, optional): What to analyze (e.g. 'Transcribe this audio', 'What sounds are in this?')
RETURNS: task_id (string) for async status tracking via get_task_status
EXAMPLE: analyze_audio(file_path="...")
NOTES: Async operation - use get_task_status to check progress.
JSON_SCHEMA:
{"type":"object","properties":{"file_path":{"type":"string","description":"Path to the audio file to analyze"},"prompt":{"type":"string","description":"What to analyze (e.g. 'Transcribe this audio', 'What sounds are in this?')"}},"required":["file_path"]}
=== END TOOL 027 — analyze_audio ===
=== START TOOL 028 — analyze_video ===
NAME: analyze_video
DESCRIPTION: Analyze a video using multimodal AI. Can describe content, identify actions, extract information.
CATEGORY: analysis
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  video_url (string, required): URL or local path to the video
  prompt (string, optional): Question or instruction about the video (default: 'Describe what happens in this video')
EXAMPLE: analyze_video(video_url="...")
JSON_SCHEMA:
{"type":"object","properties":{"video_url":{"type":"string","description":"URL or local path to the video"},"prompt":{"type":"string","description":"Question or instruction about the video (default: 'Describe what happens in this video')"}},"required":["video_url"]}
=== END TOOL 028 — analyze_video ===
=== START TOOL 029 — speech_to_text ===
NAME: speech_to_text
DESCRIPTION: Transcribe audio to text using OpenAI Whisper. Supports wav, mp3, m4a, ogg, flac, webm.
CATEGORY: audio
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  audio_path (string, required): Path to audio file (local path)
EXAMPLE: speech_to_text(audio_path="...")
JSON_SCHEMA:
{"type":"object","properties":{"audio_path":{"type":"string","description":"Path to audio file (local path)"}},"required":["audio_path"]}
=== END TOOL 029 — speech_to_text ===
=== START TOOL 030 — text_to_speech ===
NAME: text_to_speech
DESCRIPTION: Convert text to speech using OpenAI TTS HD. Returns an audio URL. Voices: alloy, echo, fable, onyx, nova, shimmer.
CATEGORY: audio
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  text (string, required): Text to convert to speech
  voice (string, optional): Voice: alloy, echo, fable, onyx, nova, shimmer (default: onyx)
  model (string, optional): Model: tts-1 or tts-1-hd (default: tts-1-hd)
RETURNS: Returns an audio URL
EXAMPLE: text_to_speech(text="...")
JSON_SCHEMA:
{"type":"object","properties":{"text":{"type":"string","description":"Text to convert to speech"},"voice":{"type":"string","description":"Voice: alloy, echo, fable, onyx, nova, shimmer (default: onyx)"},"model":{"type":"string","description":"Model: tts-1 or tts-1-hd (default: tts-1-hd)"}},"required":["text"]}
=== END TOOL 030 — text_to_speech ===
=== START TOOL 031 — list_tts_voices ===
NAME: list_tts_voices
DESCRIPTION: List available text-to-speech voices from Google Cloud TTS. Returns a large list of voices with language and gender info.
CATEGORY: audio
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  (no parameters)
RETURNS: Returns a large list of voices with language and gender info
EXAMPLE: list_tts_voices()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 031 — list_tts_voices ===
=== START TOOL 032 — gemini_pro_tts ===
NAME: gemini_pro_tts
DESCRIPTION: Generate high-quality speech using Gemini Pro TTS with 30 premium voices. Superior quality to OpenAI TTS. Supports emotional cues in text. Returns task_id (async).
CATEGORY: audio
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  text (string, required): Text to convert to speech. Include emotional cues inline (e.g. 'said with determination', 'whispered nervously').
  voice (string, optional): Voice name: Zephyr, Puck, Charon, Kore, Fenrir, Leda, Orus, Aoede, and 22 more (default: Charon)
RETURNS: task_id (string) for async status tracking via get_task_status
EXAMPLE: gemini_pro_tts(text="...")
NOTES: Async operation - use get_task_status to check progress.
JSON_SCHEMA:
{"type":"object","properties":{"text":{"type":"string","description":"Text to convert to speech. Include emotional cues inline (e.g. 'said with determination', 'whispered nervously')."},"voice":{"type":"string","description":"Voice name: Zephyr, Puck, Charon, Kore, Fenrir, Leda, Orus, Aoede, and 22 more (default: Charon)"}},"required":["text"]}
=== END TOOL 032 — gemini_pro_tts ===
=== START TOOL 033 — gmail_search ===
NAME: gmail_search
DESCRIPTION: Search or list emails in the operator's Gmail inbox. Uses Gmail search syntax (e.g., 'from:someone subject:hello', 'is:unread', 'newer_than:2d'). Leave query empty for recent emails.
CATEGORY: email
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  query (string, optional): Gmail search query like 'from:someone subject:hello'. Leave empty for recent emails
  max_results (integer, optional, default="10"): Number of results to return (1-20)
  operator (string, optional): Operator name
RETURNS: Gmail operation result
EXAMPLE: gmail_search()
JSON_SCHEMA:
{"type":"object","properties":{"query":{"type":"string","description":"Gmail search query like 'from:someone subject:hello'. Leave empty for recent emails"},"max_results":{"type":"integer","description":"Number of results to return (1-20)","default":10},"operator":{"type":"string","description":"Operator name"}},"required":[]}
=== END TOOL 033 — gmail_search ===
=== START TOOL 034 — gmail_read ===
NAME: gmail_read
DESCRIPTION: Read the full content of an email by its message ID (from gmail_search results). Returns subject, sender, body text, and labels.
CATEGORY: email
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  message_id (string, required): Gmail message ID from gmail_search results
  operator (string, optional): Operator name
RETURNS: Returns subject, sender, body text, and labels
EXAMPLE: gmail_read(message_id="...")
JSON_SCHEMA:
{"type":"object","properties":{"message_id":{"type":"string","description":"Gmail message ID from gmail_search results"},"operator":{"type":"string","description":"Operator name"}},"required":["message_id"]}
=== END TOOL 034 — gmail_read ===
=== START TOOL 035 — gmail_send ===
NAME: gmail_send
DESCRIPTION: Compose and send a new email from the operator's Gmail account.
CATEGORY: email
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  to (string, required): Recipient email address
  subject (string, required): Email subject line
  body (string, required): Email body text
  cc (string, optional): CC recipients (comma-separated email addresses)
  operator (string, optional): Operator name
RETURNS: Gmail operation result
EXAMPLE: gmail_send(to="...", subject="...", body="...")
JSON_SCHEMA:
{"type":"object","properties":{"to":{"type":"string","description":"Recipient email address"},"subject":{"type":"string","description":"Email subject line"},"body":{"type":"string","description":"Email body text"},"cc":{"type":"string","description":"CC recipients (comma-separated email addresses)"},"operator":{"type":"string","description":"Operator name"}},"required":["to","subject","body"]}
=== END TOOL 035 — gmail_send ===
=== START TOOL 036 — gmail_reply ===
NAME: gmail_reply
DESCRIPTION: Reply to an existing email thread. Automatically addresses the reply to the original sender and prefixes 'Re:' to the subject.
CATEGORY: email
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  message_id (string, required): Original message ID to reply to
  thread_id (string, required): Thread ID from the original message
  body (string, required): Reply text
  operator (string, optional): Operator name
RETURNS: Gmail operation result
EXAMPLE: gmail_reply(message_id="...", thread_id="...", body="...")
JSON_SCHEMA:
{"type":"object","properties":{"message_id":{"type":"string","description":"Original message ID to reply to"},"thread_id":{"type":"string","description":"Thread ID from the original message"},"body":{"type":"string","description":"Reply text"},"operator":{"type":"string","description":"Operator name"}},"required":["message_id","thread_id","body"]}
=== END TOOL 036 — gmail_reply ===
=== START TOOL 037 — gmail_labels ===
NAME: gmail_labels
DESCRIPTION: List all Gmail labels, or modify labels on a message (mark read/unread, archive, star/unstar).
CATEGORY: email
GROUPS: chat, chat_cu, mcp
PARAMETERS:
  action (string, required, enum=[list,mark_read,mark_unread,archive,star,unstar]): Action to perform
  message_id (string, optional): Required for modify actions (mark_read, mark_unread, archive, star, unstar)
  operator (string, optional): Operator name
RETURNS: Gmail operation result
EXAMPLE: gmail_labels(action="...")
JSON_SCHEMA:
{"type":"object","properties":{"action":{"type":"string","description":"Action to perform","enum":["list","mark_read","mark_unread","archive","star","unstar"]},"message_id":{"type":"string","description":"Required for modify actions (mark_read, mark_unread, archive, star, unstar)"},"operator":{"type":"string","description":"Operator name"}},"required":["action"]}
=== END TOOL 037 — gmail_labels ===
=== START TOOL 038 — seek_snapshot_direct ===
NAME: seek_snapshot_direct
DESCRIPTION: Directly seek and read a snapshot from the volume using byte offsets. Most efficient method for retrieving snapshot content.
CATEGORY: mcp_internal
GROUPS: mcp
PARAMETERS:
  snap_id (string, required): Snapshot ID to seek
EXAMPLE: seek_snapshot_direct(snap_id="...")
JSON_SCHEMA:
{"type":"object","properties":{"snap_id":{"type":"string","description":"Snapshot ID to seek"}},"required":["snap_id"]}
=== END TOOL 038 — seek_snapshot_direct ===
=== START TOOL 039 — mint_snapshot ===
NAME: mint_snapshot
DESCRIPTION: Create a new snapshot (memory) in the BlackBox via AI processing. Content is sent through /chat for synthesis, then auto-mint captures the result.
CATEGORY: mcp_internal
GROUPS: mcp
PARAMETERS:
  content (string, required): Content to save - will be processed by AI before minting
  operator (string, required): Operator/user name to associate with this snapshot
  snapshot_type (string, optional, default="normal"): Type hint (currently ignored - auto-mint determines type)
EXAMPLE: mint_snapshot(content="...", operator="...")
JSON_SCHEMA:
{"type":"object","properties":{"content":{"type":"string","description":"Content to save - will be processed by AI before minting"},"operator":{"type":"string","description":"Operator/user name to associate with this snapshot"},"snapshot_type":{"type":"string","description":"Type hint (currently ignored - auto-mint determines type)","default":"normal"}},"required":["content","operator"]}
=== END TOOL 039 — mint_snapshot ===
=== START TOOL 040 — get_context ===
NAME: get_context
DESCRIPTION: Get enriched context for a query, including relevant snapshots and recent history. Use before answering questions that may benefit from historical context.
CATEGORY: mcp_internal
GROUPS: mcp
PARAMETERS:
  query (string, required): The query or topic to get context for
  operator (string, required): Operator/user name
RETURNS: Requested data object
EXAMPLE: get_context(query="...", operator="...")
JSON_SCHEMA:
{"type":"object","properties":{"query":{"type":"string","description":"The query or topic to get context for"},"operator":{"type":"string","description":"Operator/user name"}},"required":["query","operator"]}
=== END TOOL 040 — get_context ===
=== START TOOL 041 — list_operators ===
NAME: list_operators
DESCRIPTION: List all operators (users) that have snapshots in the BlackBox.
CATEGORY: mcp_internal
GROUPS: mcp
PARAMETERS:
  (no parameters)
RETURNS: List of matching results
EXAMPLE: list_operators()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 041 — list_operators ===
=== START TOOL 042 — get_index_stats ===
NAME: get_index_stats
DESCRIPTION: Get statistics about the snapshot index - total count, operators, byte ranges, etc.
CATEGORY: mcp_internal
GROUPS: mcp
PARAMETERS:
  (no parameters)
RETURNS: Requested data object
EXAMPLE: get_index_stats()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 042 — get_index_stats ===
=== START TOOL 043 — browse_index ===
NAME: browse_index
DESCRIPTION: Browse the snapshot index to see available snapshots. Efficient - reads index only.
CATEGORY: mcp_internal
GROUPS: mcp
PARAMETERS:
  operator (string, optional): Filter by operator (optional)
  snap_type (string, optional): Filter by type: 'normal', 'checkpoint', 'summary' (optional)
  limit (integer, optional, default="20"): Maximum results (default: 20)
  offset (integer, optional, default="0"): Skip first N results for pagination
EXAMPLE: browse_index()
JSON_SCHEMA:
{"type":"object","properties":{"operator":{"type":"string","description":"Filter by operator (optional)"},"snap_type":{"type":"string","description":"Filter by type: 'normal', 'checkpoint', 'summary' (optional)"},"limit":{"type":"integer","description":"Maximum results (default: 20)","default":20},"offset":{"type":"integer","description":"Skip first N results for pagination","default":0}},"required":[]}
=== END TOOL 043 — browse_index ===
=== START TOOL 044 — chat_with_context ===
NAME: chat_with_context
DESCRIPTION: Send a message through BlackBox chat with full context retrieval. Returns AI response enriched with relevant snapshots.
CATEGORY: mcp_internal
GROUPS: mcp
PARAMETERS:
  message (string, required): Message to send
  operator (string, required): Operator/user name
  provider (string, optional, default="anthropic"): AI provider: 'anthropic', 'openai', or 'gemini'
  model (string, optional): Model name (optional - uses default for provider)
RETURNS: Returns AI response enriched with relevant snapshots
EXAMPLE: chat_with_context(message="...", operator="...")
JSON_SCHEMA:
{"type":"object","properties":{"message":{"type":"string","description":"Message to send"},"operator":{"type":"string","description":"Operator/user name"},"provider":{"type":"string","description":"AI provider: 'anthropic', 'openai', or 'gemini'","default":"anthropic"},"model":{"type":"string","description":"Model name (optional - uses default for provider)"}},"required":["message","operator"]}
=== END TOOL 044 — chat_with_context ===
=== START TOOL 045 — refresh_index ===
NAME: refresh_index
DESCRIPTION: Force refresh the snapshot index cache. Use after minting new snapshots.
CATEGORY: mcp_internal
GROUPS: mcp
PARAMETERS:
  (no parameters)
EXAMPLE: refresh_index()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 045 — refresh_index ===
=== START TOOL 046 — get_music_status ===
NAME: get_music_status
DESCRIPTION: Check the status of a music generation task. Alias for get_task_status with music-specific context.
CATEGORY: mcp_internal
GROUPS: mcp
PARAMETERS:
  task_id (string, required): The task ID returned by generate_music
RETURNS: Requested data object
EXAMPLE: get_music_status(task_id="...")
JSON_SCHEMA:
{"type":"object","properties":{"task_id":{"type":"string","description":"The task ID returned by generate_music"}},"required":["task_id"]}
=== END TOOL 046 — get_music_status ===
=== START TOOL 047 — toolvault ===
NAME: toolvault
DESCRIPTION: Your tool discovery system. Use this to find and retrieve tools from the ToolVault. Actions: 'search' finds tools by what you need to do, 'read' gets the full spec for a specific tool, 'list' shows all available tools by category.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  action (string, required, enum=[search,read,list]): search=find tools by query, read=get full tool spec by name, list=show all tools by category
  query (string, optional): For search: natural language description of what you need (e.g., 'send a text message', 'generate an image')
  tool_name (string, optional): For read: exact tool name to retrieve (e.g., 'send_sms', 'generate_image')
  category (string, optional): For list: filter by category (e.g., 'communication', 'media_generation', 'email'). Omit for all.
EXAMPLE: toolvault(action="...")
JSON_SCHEMA:
{"type":"object","properties":{"action":{"type":"string","enum":["search","read","list"],"description":"search=find tools by query, read=get full tool spec by name, list=show all tools by category"},"query":{"type":"string","description":"For search: natural language description of what you need (e.g., 'send a text message', 'generate an image')"},"tool_name":{"type":"string","description":"For read: exact tool name to retrieve (e.g., 'send_sms', 'generate_image')"},"category":{"type":"string","description":"For list: filter by category (e.g., 'communication', 'media_generation', 'email'). Omit for all."}},"required":["action"]}
=== END TOOL 047 — toolvault ===
=== START TOOL 048 — ugv_motion_move_forward ===
NAME: ugv_motion_move_forward
DESCRIPTION: Drive the UGV Beast — a physical Waveshare tracked robot you can control — straight forward for a duration. The robot has a JBL speaker for your voice and cameras for your sight. Safety-clamped server-side: max 0.15 m/s, max 10 seconds per call. Always check LiDAR with ugv_status_get_lidar_summary before moving if you haven't seen a recent reading. If any front sector is under 0.3 m, do NOT move forward.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  duration_s (number, required): Seconds to drive. Clamped at 10.
  speed_m_s (number, optional, default="0.1"): Linear speed in m/s (max 0.15).
EXAMPLE: ugv_motion_move_forward(duration_s="...")
JSON_SCHEMA:
{"type":"object","properties":{"duration_s":{"type":"number","description":"Seconds to drive. Clamped at 10.","minimum":0.1,"maximum":10.0},"speed_m_s":{"type":"number","description":"Linear speed in m/s (max 0.15).","minimum":0.02,"maximum":0.15,"default":0.1}},"required":["duration_s"]}
=== END TOOL 048 — ugv_motion_move_forward ===
=== START TOOL 049 — ugv_motion_move_backward ===
NAME: ugv_motion_move_backward
DESCRIPTION: Drive the UGV Beast tracked robot straight backward for a duration. Safety-clamped: max 0.15 m/s, max 10 s. Blind reverse is riskier than forward — prefer short (<2s) pulses and confirm back-sector LiDAR with ugv_status_get_lidar_summary first.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  duration_s (number, required): Seconds to drive.
  speed_m_s (number, optional, default="0.08"): Linear speed in m/s (max 0.15).
EXAMPLE: ugv_motion_move_backward(duration_s="...")
JSON_SCHEMA:
{"type":"object","properties":{"duration_s":{"type":"number","minimum":0.1,"maximum":10.0,"description":"Seconds to drive."},"speed_m_s":{"type":"number","minimum":0.02,"maximum":0.15,"default":0.08,"description":"Linear speed in m/s (max 0.15)."}},"required":["duration_s"]}
=== END TOOL 049 — ugv_motion_move_backward ===
=== START TOOL 050 — ugv_motion_rotate_left ===
NAME: ugv_motion_rotate_left
DESCRIPTION: Rotate the UGV Beast tracked robot in place counter-clockwise (positive angular velocity). Useful for scanning the environment or aligning heading. Safety-clamped: max 0.8 rad/s, max 5 seconds. Check LiDAR with ugv_status_get_lidar_summary if the robot is close to obstacles — rotation sweeps the gimbal arm through nearby space.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  duration_s (number, required): Seconds to rotate.
  rate_rad_s (number, optional, default="0.5"): Angular rate in rad/s (max 0.8).
EXAMPLE: ugv_motion_rotate_left(duration_s="...")
JSON_SCHEMA:
{"type":"object","properties":{"duration_s":{"type":"number","minimum":0.1,"maximum":5.0,"description":"Seconds to rotate."},"rate_rad_s":{"type":"number","minimum":0.1,"maximum":0.8,"default":0.5,"description":"Angular rate in rad/s (max 0.8)."}},"required":["duration_s"]}
=== END TOOL 050 — ugv_motion_rotate_left ===
=== START TOOL 051 — ugv_motion_rotate_right ===
NAME: ugv_motion_rotate_right
DESCRIPTION: Rotate the UGV Beast tracked robot in place clockwise (negative angular velocity). Useful for scanning the environment or aligning heading. Safety-clamped: max 0.8 rad/s, max 5 seconds. Check LiDAR with ugv_status_get_lidar_summary if the robot is close to obstacles.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  duration_s (number, required): Seconds to rotate.
  rate_rad_s (number, optional, default="0.5"): Angular rate in rad/s (max 0.8).
EXAMPLE: ugv_motion_rotate_right(duration_s="...")
JSON_SCHEMA:
{"type":"object","properties":{"duration_s":{"type":"number","minimum":0.1,"maximum":5.0,"description":"Seconds to rotate."},"rate_rad_s":{"type":"number","minimum":0.1,"maximum":0.8,"default":0.5,"description":"Angular rate in rad/s (max 0.8)."}},"required":["duration_s"]}
=== END TOOL 051 — ugv_motion_rotate_right ===
=== START TOOL 052 — ugv_motion_stop ===
NAME: ugv_motion_stop
DESCRIPTION: Immediately stop all UGV Beast motion by publishing a zero-velocity twist. Safe to call at any time; idempotent. Use this any time you want the robot to halt — don't wait for duration timers.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_motion_stop()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 052 — ugv_motion_stop ===
=== START TOOL 053 — ugv_gimbal_look_at ===
NAME: ugv_gimbal_look_at
DESCRIPTION: Point the UGV Beast's pan-tilt gimbal camera to absolute pan and tilt angles in degrees. The gimbal sits on top of the robot and carries the primary camera. Pan: -180 to +180 (negative=right, positive=left, zero=forward). Tilt: -45 to +90 (negative=down, positive=up). Servos are open-loop — commanded position is effectively actual after ~500ms settle.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  pan_deg (number, required): Pan angle in degrees. Negative=right, positive=left, zero=forward.
  tilt_deg (number, required): Tilt angle in degrees. Negative=down, positive=up.
  speed (integer, optional, default="100"): Servo speed 1-300. 100 is a gentle default.
EXAMPLE: ugv_gimbal_look_at(pan_deg="...", tilt_deg="...")
JSON_SCHEMA:
{"type":"object","properties":{"pan_deg":{"type":"number","minimum":-180,"maximum":180,"description":"Pan angle in degrees. Negative=right, positive=left, zero=forward."},"tilt_deg":{"type":"number","minimum":-45,"maximum":90,"description":"Tilt angle in degrees. Negative=down, positive=up."},"speed":{"type":"integer","minimum":1,"maximum":300,"default":100,"description":"Servo speed 1-300. 100 is a gentle default."}},"required":["pan_deg","tilt_deg"]}
=== END TOOL 053 — ugv_gimbal_look_at ===
=== START TOOL 054 — ugv_gimbal_reset ===
NAME: ugv_gimbal_reset
DESCRIPTION: Return the UGV Beast's pan-tilt gimbal to the forward-center home position (pan=0, tilt=0). Call this to "look straight ahead again" after scanning.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_gimbal_reset()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 054 — ugv_gimbal_reset ===
=== START TOOL 055 — ugv_gimbal_get_state ===
NAME: ugv_gimbal_get_state
DESCRIPTION: Get the current commanded pan and tilt angles of the UGV Beast's gimbal. Returns the last-commanded position (servos are open-loop PWM — no actual encoder feedback available on this hardware).
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
RETURNS: Returns the last-commanded position (servos are open-loop PWM — no actual encoder feedback available
EXAMPLE: ugv_gimbal_get_state()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 055 — ugv_gimbal_get_state ===
=== START TOOL 056 — ugv_camera_list ===
NAME: ugv_camera_list
DESCRIPTION: List all cameras available on the UGV Beast — a pan-tilt mounted USB camera ("pantilt") and an OAK-D Lite depth camera on top ("oakd") — and report whether each is currently streaming fresh frames.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_camera_list()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 056 — ugv_camera_list ===
=== START TOOL 057 — ugv_camera_snapshot ===
NAME: ugv_camera_snapshot
DESCRIPTION: Capture the latest JPEG frame from one of the UGV Beast's cameras so you can "see" what the robot sees. Set as_url=true for a fast URL response, or as_url=false to receive the image as base64 (useful when you need to analyze the image yourself via vision).
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  camera (string, required, enum=[pantilt,oakd]): Which camera: 'pantilt' for the pan-tilt camera, 'oakd' for the OAK-D Lite.
  as_url (boolean, optional, default="False"): If true, return a URL like /snapshot/pantilt. If false, return base64 image bytes.
EXAMPLE: ugv_camera_snapshot(camera="...")
JSON_SCHEMA:
{"type":"object","properties":{"camera":{"type":"string","enum":["pantilt","oakd"],"description":"Which camera: 'pantilt' for the pan-tilt camera, 'oakd' for the OAK-D Lite."},"as_url":{"type":"boolean","default":false,"description":"If true, return a URL like /snapshot/pantilt. If false, return base64 image bytes."}},"required":["camera"]}
=== END TOOL 057 — ugv_camera_snapshot ===
=== START TOOL 058 — ugv_status_get_pose ===
NAME: ugv_status_get_pose
DESCRIPTION: Get the UGV Beast's current (x, y, yaw) pose in the map frame. Yaw is in both radians and degrees. Use this to know where the robot is before planning navigation or describing location.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_status_get_pose()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 058 — ugv_status_get_pose ===
=== START TOOL 059 — ugv_status_get_odom ===
NAME: ugv_status_get_odom
DESCRIPTION: Get the UGV Beast's filtered odometry: position, heading, and current linear/angular velocity. Velocities are the actual observed values (non-zero while moving).
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_status_get_odom()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 059 — ugv_status_get_odom ===
=== START TOOL 060 — ugv_status_get_lidar_summary ===
NAME: ugv_status_get_lidar_summary
DESCRIPTION: Summarize the UGV Beast's 360° LiDAR scan as 8 directional sectors (front, front_left, left, back_left, back, back_right, right, front_right) with the minimum distance in meters per sector plus an overall minimum. Call this before any motion to check for obstacles — robot radius is ~0.2 m, so distances under 0.3 m are danger-close.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_status_get_lidar_summary()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 060 — ugv_status_get_lidar_summary ===
=== START TOOL 061 — ugv_status_list_nodes ===
NAME: ugv_status_list_nodes
DESCRIPTION: List all running ROS2 nodes on the UGV Beast. Useful for diagnostics — healthy robot should have ~30+ nodes including bt_navigator, controller_server, planner_server, slam_toolbox.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_status_list_nodes()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 061 — ugv_status_list_nodes ===
=== START TOOL 062 — ugv_status_list_topics ===
NAME: ugv_status_list_topics
DESCRIPTION: List all active ROS2 topics on the UGV Beast with their message types. Useful for diagnostics when a subsystem seems offline.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_status_list_topics()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 062 — ugv_status_list_topics ===
=== START TOOL 063 — ugv_status_health ===
NAME: ugv_status_health
DESCRIPTION: Overall UGV Beast health report: whether the ROS bridge is running plus freshness (in seconds since last message) for all key topics — /odom, /scan, /robot_pose, /map, camera streams, gimbal state. A healthy robot shows fresh under 0.5 s for all topics except /map (which updates sparingly).
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_status_health()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 063 — ugv_status_health ===
=== START TOOL 064 — ugv_nav_goto_point ===
NAME: ugv_nav_goto_point
DESCRIPTION: Navigate the UGV Beast to an (x, y, yaw) pose in the map frame using Nav2 autonomous path planning. This is the preferred long-distance movement — Nav2 handles obstacle avoidance and path finding. Non-blocking: returns immediately after goal is accepted; poll ugv_nav_status for progress. Cancel with ugv_nav_cancel.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  x (number, required): Goal x coordinate in the map frame (meters).
  y (number, required): Goal y coordinate in the map frame (meters).
  yaw_deg (number, optional, default="0.0"): Desired goal heading in degrees (-180 to 180).
RETURNS: returns immediately after goal is accepted; poll ugv_nav_status for progress
EXAMPLE: ugv_nav_goto_point(x="...", y="...")
JSON_SCHEMA:
{"type":"object","properties":{"x":{"type":"number","description":"Goal x coordinate in the map frame (meters)."},"y":{"type":"number","description":"Goal y coordinate in the map frame (meters)."},"yaw_deg":{"type":"number","minimum":-180,"maximum":180,"default":0.0,"description":"Desired goal heading in degrees (-180 to 180)."}},"required":["x","y"]}
=== END TOOL 064 — ugv_nav_goto_point ===
=== START TOOL 065 — ugv_nav_cancel ===
NAME: ugv_nav_cancel
DESCRIPTION: Cancel the UGV Beast's currently active Nav2 navigation goal. Idempotent — returns canceled:false if there was no active goal.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
RETURNS: returns canceled:false if there was no active goal
EXAMPLE: ugv_nav_cancel()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 065 — ugv_nav_cancel ===
=== START TOOL 066 — ugv_nav_status ===
NAME: ugv_nav_status
DESCRIPTION: Get the UGV Beast's current Nav2 goal status (idle, navigating, succeeded, aborted, canceled) and the distance remaining in meters to the goal.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_nav_status()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 066 — ugv_nav_status ===
=== START TOOL 067 — ugv_system_emergency_stop ===
NAME: ugv_system_emergency_stop
DESCRIPTION: Emergency stop the UGV Beast: publishes a zero-velocity twist AND sends an ESP32 all-motor cutoff command. Reserve for actual emergencies — for normal halts use ugv_motion_stop instead. This is the panic button to hit when you suspect runaway motion or an imminent collision.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_system_emergency_stop()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 067 — ugv_system_emergency_stop ===
=== START TOOL 068 — ugv_system_servo_center ===
NAME: ugv_system_servo_center
DESCRIPTION: Center the UGV Beast's servos to mid-point. Primarily a calibration operation — returns the gimbal to pan=0, tilt=0.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
RETURNS: returns the gimbal to pan=0, tilt=0
EXAMPLE: ugv_system_servo_center()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 068 — ugv_system_servo_center ===
=== START TOOL 069 — ugv_system_servo_release ===
NAME: ugv_system_servo_release
DESCRIPTION: Release UGV Beast servo torque (limp mode). After this, the gimbal will flop under gravity — it won't hold its position. Only use when you specifically want free manipulation or storage.
CATEGORY: uncategorized
GROUPS: chat, chat_cu, realtime, gemini_live, grok_live, phone, mcp
PARAMETERS:
  (no parameters)
EXAMPLE: ugv_system_servo_release()
JSON_SCHEMA:
{"type":"object","properties":{},"required":[]}
=== END TOOL 069 — ugv_system_servo_release ===
